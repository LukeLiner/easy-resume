---
description: Reactive Resume 编码规范（极致精简高性能版）- 适用于 DeepSeek
alwaysApply: true
enabled: true
---

# 思维控制（最高优先级）
- **禁止自我质疑**：不需要在内部反复检查是否遵循了每一条规则，直接按规则编码。
- **快速决策**：遇到规则冲突或模糊点时，选择最直接、最安全、最少依赖的实现方案，不追加额外假设或过度设计。
- **一次性输出**：仅输出最终代码和一句简短总结，不要输出思考过程、不要列出检查清单、不要重复规则内容。
- **严格限定输出长度**：单次回复代码改动 ≤150 行，若超限则分多次回复；每次回复必须独立可运行，不依赖未提交的前置修改。

# 核心执行指令
1. **原子修改**：只改被请求的代码，禁止改动未授权签名、参数名、默认值。
2. **生成文件禁止触碰**：严禁手动编辑 `routeTree.gen.ts` 等任何自动生成文件。

# 编码硬性铁律（快速应用，不反复斟酌）
3. **类型安全**：所有函数参数/返回值必须显式类型注解。禁止 `any`。React 组件 Props 必须使用命名的 `type` 或 `interface`，禁止内联对象。
4. **导入规范**：Workspace 内依赖必须通过包名引用（如 `@reactive-resume/ui`），**绝对禁止**使用相对路径或 `src` 穿透引入其他 workspace 源码。
5. **国际化（i18n）**：所有用户可见文案必须用 `t` macro（`@lingui/core/macro`）包裹，禁止硬编码字符串。

# 架构与安全速查（直接决策，不展开讨论）
6. **代码归宿**：UI/Hook → `ui`；路由/页面 → `web/features`；DB Schema → `db/schema`；API 过程 → `api/features`；纯简历逻辑 → `resume`；PDF → `pdf`。
7. **环境变量**：新增 env 必须同步添加到 `turbo.json` 的 `globalEnv` 数组。
8. **错误与安全**：禁止空 `catch`。禁止硬编码密钥。用户输入必须经 Zod 校验。
9. **Agent 文档同步**：涉及 `agent-chat` 逻辑修改，必须同步更新 `docs/agent-chat.md`。

# 特别提醒
- 无需向用户确认“是否合规”，直接交付高质量、符合上述规则的代码即可。
- 若任务复杂，主动分步（先类型/接口，后逻辑，最后 i18n 与错误处理）以控制单次输出长度。
- **若某条规则与“思维控制”冲突，以“思维控制”为准，直接执行，不再递归分析。**