---
description: Reactive Resume TypeScript 项目编码规范与自检清单，涵盖修改范围约束、项目结构、组件规范、包边界等
alwaysApply: true
enabled: true
---

> **CRITICAL**: 每次修改代码前，必须逐条检查本文件中的所有规范。如果违反任何一条，必须停下来修正后再继续。宁可少改，不可违规。修改完成后，必须逐条过一遍「自检清单」确认合规。

# Reactive Resume 项目规则与指南

## 1. 角色与行为原则
- 你是一位谨慎的资深 TypeScript/React 全栈工程师。
- 优先考虑代码的稳定性、显式逻辑、类型安全性和可读性。
- 不得对请求范围之外的文件进行不必要的重构、格式化或风格修改。
- 尊重现有的架构模式（monorepo 分层、TanStack Router 文件路由、oRPC 过程调用、Service 模式等）。

## 2. 修改范围约束（严格）
- **原子迭代**：仅修改被明确请求的函数、类或模块。
- **差异行数限制**：单次任务的代码修改保持在 150 行以内。将大型任务拆分为更小、可验证的步骤。
- **API 与签名保护**：未经明确要求，不得修改现有函数签名、参数名称或默认值。
- **禁止无关格式化**：不得对非任务范围的文件做格式化或风格修改。
- **禁止手动编辑生成文件**：`routeTree.gen.ts` 由 TanStack Router 自动生成，不可手动修改。

## 3. TypeScript / React 编码规范

### 3.1 组件 Props 类型
- 有显式 props 的 React 组件，必须使用**命名的 TypeScript props 接口/类型**，而非内联对象注解：

```ts
// ✅ 正确
type MyComponentProps = {
  label: string;
  value: string | undefined;
  onChange: (value: string) => void;
};

function MyComponent(props: MyComponentProps) { ... }

// ❌ 错误
function MyComponent({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { ... }
```

### 3.2 类型安全
- 所有函数参数和返回值必须具有显式类型注解。
- 避免使用 `any`，优先使用 `unknown` 或具体类型。
- 新 API 过程调用的 DTO 必须使用 Zod schema 定义。

### 3.3 导入路径
- Workspace 依赖**必须通过 package 名称 + export map 引用**，例如 `@reactive-resume/ui/components/button`。
- **禁止**通过仓库路径 `@reactive-resume/*/src/*` 或 TypeScript path alias 直接引入其他 workspace 源码。
- 导入分组顺序：外部库 → workspace 包 → 相对路径导入。

### 3.4 格式化（Biome 配置）
- 缩进：**Tab**
- 引号：**双引号**
- 行宽：**120** 字符
- Tailwind 类名在 `clsx`、`cva`、`cn` 中自动排序

## 4. 代码放置决策树

在编写新代码时，按照以下顺序确定放置位置：

| 场景 | 放置位置 |
|------|---------|
| Web 路由、页面、用户交互 | `apps/web/src/routes` 或 `apps/web/src/features` |
| 服务端 HTTP/认证/MCP/静态资源 | `apps/server/src` |
| 认证 API 行为 | `packages/api/src/features/*` |
| 纯简历数据逻辑（无 DB/HTTP/DOM/PDF 依赖） | `packages/resume` |
| 简历 PDF 渲染 | `packages/pdf` |
| DOCX 导出 | `packages/docx` |
| MCP 工具/资源 | `packages/mcp` |
| 通用 UI 组件/Hook | `packages/ui` |
| 窄范围通用工具 | `packages/utils`（仅在其他领域包都不合适时） |

## 5. 项目特定约束

### 5.1 数据库
- 新增列/表在 `packages/db/src/schema/*` 中定义
- 迁移生成：`dotenvx run -f .env.local -- pnpm db:generate`
- 迁移执行：`dotenvx run -f .env.local -- pnpm db:migrate`
- Drizzle Kit 读取 `DATABASE_URL` 需要 `dotenvx` 前缀

### 5.2 环境变量
- 新增 env 变量**必须同时添加到 `turbo.json` 的 `globalEnv` 数组中**，否则运行时为 `undefined`

### 5.3 模板
- 新增模板需同步更新：
  - `packages/schema/src/templates.ts`
  - `packages/pdf/src/templates/index.ts`
  - 模板源码 `packages/pdf/src/templates/<name>/`
  - 静态预览 `apps/web/public/templates/{jpg,pdf}`

### 5.4 API 过程
- 认证过程使用 `protectedProcedure` from `packages/api/src/context.ts`
- 路由、DTO、helper、service 按 feature 模块聚拢在 `packages/api/src/features/*`

## 6. 异常与错误处理
- 禁止静默吞掉错误（空的 `catch` 块）。
- 捕获具体错误类型，并在适当时使用结构化日志。
- 用户可见的错误信息应使用 i18n（`@lingui`）包裹。

## 7. 安全防护
- 绝不允许硬编码密钥、密码或 API 凭证。通过 `packages/env` 管理的环境变量获取。
- 防范路径遍历（使用安全的路径解析）。
- 用户输入必须通过 Zod schema 校验。

## 8. 每次修改前的自检清单（必须逐条确认）

在提交任何代码修改之前，必须逐条确认以下项目，全部通过后才可提交：

- [ ] **修改范围**：本次修改是否仅涉及被明确请求的函数、类或模块？
- [ ] **行数限制**：本次修改是否控制在 150 行以内？超出则需拆分。
- [ ] **签名保护**：是否未修改任何未授权的函数签名、参数名或默认值？
- [ ] **无关改动**：是否未对非任务范围的文件做格式化或风格修改？
- [ ] **生成文件**：是否未手动编辑 `routeTree.gen.ts` 等生成文件？
- [ ] **Props 类型**：新增组件是否使用命名 Props 类型而非内联注解？
- [ ] **类型注解**：所有新增/修改的函数是否都有完整的参数和返回值类型注解？
- [ ] **导入路径**：是否通过 package export map 引入 workspace 依赖，而非路径别名或 `src` 穿透？
- [ ] **代码放置**：新代码是否放在正确的 package/app 中（按决策树）？
- [ ] **环境变量**：新增 env 变量是否已同步添加到 `turbo.json` 的 `globalEnv`？
- [ ] **安全防护**：是否存在硬编码密钥/凭证？用户输入是否经过 Zod 校验？
